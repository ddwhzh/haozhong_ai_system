"""Generation Agent — proposal-based template extraction (Fast RCNN inspired).

Converts complex generation tasks into controlled extraction tasks via
business process proposal decomposition, ensuring granularity consistency.
"""

from app.agents.generation.agent import GenerationAgent

__all__ = ["GenerationAgent"]
