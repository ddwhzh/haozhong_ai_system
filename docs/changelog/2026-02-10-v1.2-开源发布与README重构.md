# 2026-02-10 v1.2 开源发布与 README 重构

## 1. 变更目标

- 将当前项目推送到公开 GitHub 仓库 `ddwhzh/haozhong_ai_system`。
- 将仓库首页 README 从模板描述升级为与当前实现一致的项目说明。
- 排查并修复发布前的显性密钥风险。

## 2. 本次已实现的事实变更

1) README 重写  
- 文件: `README.md`  
- 变化: 更新为当前多Agent系统的真实架构、启动方式、API 路由、测试命令、文档索引与安全说明。  

2) 发布前密钥风险修复  
- 文件: `scripts/seed_data.py`  
- 变化: 去除 `OPENAI_API_KEY` 的硬编码默认值, 改为仅从环境变量读取。  
- 变化: 在 `get_embeddings()` 中增加空密钥显式报错, 防止静默失败。  

## 3. 假设与边界

- 假设 A: 当前仓库中的 staged 代码即为用户希望一起发布的内容。  
- 假设 B: 目标远程仓库由当前终端环境具备 push 权限。  
- 假设 C: README 以中文为主更符合当前项目文档体系。  

## 4. 未实现或延期项

- 未新增 CI workflow 与 release tag 流程, 本次仅完成首次推送与文档对齐。
- 未调整现有业务逻辑与接口契约, 仅做文档和安全性最小必要改动。

## 5. 回滚方案

- 若需回滚文档改动, 可按提交粒度回退 `README.md` 与本文件。
- 若需恢复脚本旧行为, 仅需回滚 `scripts/seed_data.py` 中 API key 默认值与校验逻辑。
